<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title><%= title %></title>
    <!-- CSS -->
    <link href="./styles/root.css" rel="stylesheet">
    <!-- styles for the individual pages -->
    <!-- make sure these are in the 'public/styles/' directory -->
    <% if (locals.css) { %>
      <link rel="stylesheet" href="./pages/<%= css %>.css">
    <% } %>
  </head>
  <body>
    <header>
      <h2>Data Drive</h2>
    </header>
    <main>
      <!-- the pages view is just the main content -->
      <!-- make sure these are in 'views/pages/' directory  -->
      <% if (locals.page) { %>
        <%- include(`./pages/${page}.ejs`) %>
      <% } %>
    </main>
    <footer></footer>
  </body>
</html>
